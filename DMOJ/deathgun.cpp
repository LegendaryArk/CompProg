#include <bits/stdc++.h>
using namespace std;

const int MM = 2000;

int M, in[MM], ind;
string a, b = 0;
vector<string> name;
map<string, int> mp;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	
}